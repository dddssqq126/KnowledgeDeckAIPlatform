# Dev-only dependencies (testing). Not required for production runtime.
# Install in Docker via `pip install -r requirements-dev.txt` (in addition
# to requirements.txt) so `docker compose exec backend pytest` works.

-r requirements.txt
pytest>=8.3.0
pytest-asyncio>=0.24.0
testcontainers[postgresql]>=4.8.0
# Tests run migrations + ORM against a real Postgres testcontainer (see
# conftest.py), so they need the psycopg driver even though the runtime
# now uses aiosqlite.
psycopg[binary]>=3.2.0
